# whatsapp-flows-endpoint

Minimal HTTPS endpoint for Meta WhatsApp Flows **data exchange** calls.

## Endpoints

- `GET /health` – health check
- `POST /wa/flows` – placeholder handler (will be updated to the exact Flow contract)

## Run locally

```bash
npm install
PORT=8080 npm start
```
